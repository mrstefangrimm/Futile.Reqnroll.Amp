kind: BuildConfig
apiVersion: build.openshift.io/v1
metadata:
  name: s2i-webcalculator
  namespace: futile-tests
spec:
  output:
    to:
      kind: ImageStreamTag
      name: 's2i-webcalculator:latest'
  successfulBuildsHistoryLimit: 5
  failedBuildsHistoryLimit: 5
  strategy:
    type: Docker
    dockerStrategy:
      dockerfilePath: Dockerfile
  source:
    type: Git
    git:
      uri: 'https://github.com/mrstefangrimm/Futile.Reqnroll.Amp'
      ref: main
    contextDir: tests/WebCalculator
  runPolicy: Serial
